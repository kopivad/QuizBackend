package com.kopivad.quizzes.domain;

public enum QuestionType {
    SINGLE, // One correct answer
    MULTI, // More than one correct answer
    TEXT, // Answer based on user`s input text
    NUMBER // Answer based on user`s input number
}
